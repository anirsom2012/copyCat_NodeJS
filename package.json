{
  "name": "webhook_echo_sample",
  "version": "1.0.0",
  "description": "A simple webhook which will get request from api.ai and will response with the same message.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anirban Som",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2"
  }
}
